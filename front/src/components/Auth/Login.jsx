import React from "react";
import Typography from "@material-ui/core/Typography";

export const Login = () => {
    return(
        <Typography variant="h6">
            Login
        </Typography>
    )
};